extends layout

block content
  h1= title
  p Welcome to #{title} Assignment
  button(type="button", class="btn btn-secondary", onclick = "update()") press me
   br
  br
  div(id = "target") 
    b Display
  
  script.  
   var client_count=0;
     function update() {
       client_count++;
       document.getElementById("target").innerHTML=`Value now ${client_count}`;
     }

